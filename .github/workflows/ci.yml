name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint-test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - name: Install dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
      - name: Ruff lint
        run: ruff check .
      - name: Ruff format check
        run: ruff format --check .
      - name: Mypy (non-blocking)
        run: mypy app || true
      - name: Bandit (security)
        run: bandit -r app -ll
      - name: Semgrep (OWASP/SAST)
        run: semgrep --config=p/python --config=p/owasp-top-ten --error app
      - name: pip-audit (deps)
        # PYSEC-2026-178: pyjwt<2.13.0 CVE, pulled in transitively by semgrep
        # (semgrep pins pyjwt~=2.12.0, so we can't upgrade). Dev-tooling only — not
        # shipped in the app. Remove this ignore once semgrep relaxes its pyjwt pin.
        run: pip-audit -r requirements-dev.txt --ignore-vuln PYSEC-2026-178
      - name: Pytest (non-gui)
        run: pytest -m "not gui" -v

  gui-test-windows:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-dev.txt
      - name: Install dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
      - name: Pytest (full incl. gui)
        run: pytest -v
